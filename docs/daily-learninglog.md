# daily-learninglog

# Daily learning log

What I’ve learned while building this project, logged the day it comes up.
Not a decision record (that’s `data-decisions.md`) and not a metric spec
(that’s `metric-changelog.md`) — this is concepts: things explained, mistakes
made, what actually clicked.

Format: one row per concept in the index table below, newest at the bottom.
`id` links to an expanded entry underneath the table when a code example is
too long to read in a cell — short one-liners just live in the cell.

**Note on ordering.** `L-007` onward are *backfilled* from Phase 0 and early
Phase 1, so their dates are earlier than `L-001`–`L-006`. IDs are assignment
order and are never reused or renumbered, which means id order and date order
diverge from here on. Sort on `date` if you want the chronology.

| id | date | topic | concept | question_asked | explanation_summary | code_example | where_used | source | follow_up_needed |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| L-001 | 2026-08-04 | pandas / display config | Global display options control what you can *see*, not what the data actually contains | “What does pd.set_option() do” | `pd.set_option()` changes session-wide formatting (`max_columns`, `width`, `max_colwidth`, precision) — it never touches underlying values. Pandas’ defaults truncate wide/long tables silently (~20 cols, ~80 char width), which can hide the exact column holding a data-quality issue. `pd.option_context()` gives the same control scoped to one block instead of the whole session. | `pd.set_option("display.max_columns", None)pd.set_option("display.width", 200)` | `01_data_audit.ipynb` setup cell | `pd.set_option` question | none |
| L-002 | 2026-08-04 | notebooks / execution order | Cell position on the page is not execution order — a `NameError` on a defined-looking variable usually means an earlier cell never ran | “I ran into nameerror, how to fix it” | Jupyter runs cells in whatever order you click them, not top-to-bottom. `cgm_files` failed because the cell that built it either hadn’t run yet in this kernel, or the kernel was restarted and lost all prior state. Restart-and-run-all is the reliable check. | `for f in cgm_files: ...` → `NameError: name 'cgm_files' is not defined` | `01_data_audit.ipynb` | NameError debugging | none |
| L-003 | 2026-08-04 | pandas fundamentals | Boolean masking — a True/False Series inside `[...]` keeps matching labels and drops the rest, matched by index label, not position | “does that mean pandas only keeps the data that is True” | `out[out > 0]` is two steps: `out > 0` builds a same-index True/False Series, then `out[mask]` returns only rows where the mask is True. Alignment is by label, which is why the mask has to share the exact index of what it’s filtering. | `mask = out > 0out[mask]` | `missingness()` sparse-column filtering | boolean masking walkthrough | none |
| L-004 | 2026-08-04 | pandas / functions | A function’s local variable disappears after it returns, unless the function explicitly `return`s it | “What’s the missingness_series represent” | `missingness()` printed but never returned `out`, so calling `missingness(t2, ...)` and then referencing `missingness_series` afterward is a `NameError` waiting to happen — same family of bug as L-017, different cause. Fix: add `return out` at the end. | `def missingness(df, name): ...; return outmissingness_series = missingness(t2, "T2DM summary")` | `missingness()` rewrite | `missingness_series` confusion | none |
| L-005 | 2026-08-05 | pandas / grouping | `DataFrame.value_counts()` on more than one selected column counts unique *combinations*, not each column separately | “teach me how to read `lost = t2[...][hypo_cols].value_counts()`” | `hypo_cols` with 2 columns turns `.value_counts()` into row-combination counts (medication list × yes/no) keyed on tuples — not what was intended. Narrowing to the single confirmed outcome column gives the clean `{no: N, yes: M}` split, which is what lets this pattern check whether missingness in a column skews toward one class of the outcome. | `t2[t2[col].isna()][["Hypoglycemia (yes/no)"]].value_counts()` | sparse-column vs. outcome cross-check | `lost` value_counts breakdown | none |
| L-006 | 2026-08-05 | dataset provenance | An outcome label’s collection method has to be checked against the source paper, not assumed from the column name | “did they mention how the col Hypoglycemia (yes/no) is collected” | Zhao et al. 2023 (the dataset’s Scientific Data descriptor) states hypoglycemia occurrence was collected via questionnaire/medical-record review, separate from the CGM device — ruling out label = formula-on-CGM-trace. Exact case definition and time window vs. the CGM recording are unstated, so same-visit features may still be concurrently associated rather than temporally predictive. | n/a — literature check, not code | build plan review; hypoglycemia risk model scoping | data descriptor PDF read | log in `data-decisions.md` — **do not use D-005, that ID is already HbA1c units (L-009); use the next real open ID** |
| L-007 | 2026-08-05 | modeling practice | “Predicts” and “is associated with” are different claims — wording has to match what the data structure can support | “would that change how I build the prediction model later on” | Same-visit label + same-visit features means the honest claim is concurrent association, not prospective forecasting. Concretely: run a clinical-only vs. clinical+CGM feature ablation to see how much signal is CGM-driven, and check whether diabetes type alone separates the classes (T1DM cohort is 14 yes / 2 no) before trusting SHAP rankings. | n/a — planning, not code yet | Phase 4 planning; `findings.md` framing | model framing discussion | revisit once Phase 4 ablation is actually run |
| L-008 | 2026-08-05 | modeling / evaluation | Class balance has to be checked per subgroup, not just overall — a pooled ratio can hide two opposite subgroup patterns | “What is the class balance?” | Combined hypoglycemia split is 80.8% no / 19.2% yes, but T1DM alone is inverted (87.5% yes) against T2DM’s 90.8% no. Pooling without accounting for cohort risks a model that’s really learning diabetes type rather than the intended clinical drivers; T1DM’s 16 rows leave almost no room for a reliable train/test split on its own. | `t2["Hypoglycemia (yes/no)"].value_counts(dropna=False, normalize=True)` | Phase 4 planning; build plan review | class balance computation | none |
| L-009 | 2026-08-05 | pathlib | A relative path only means one thing once you know the working directory it’s relative *from* | “what does Path(‘../data/raw’).resolve() mean” | `Path(...)` just stores the string; `.resolve()` is what actually asks the filesystem for the absolute location, walking up from `Path.cwd()`. The same relative string resolves to two different real folders depending on whether the notebook’s cwd is `notebooks/` or the repo root — a silent wrong-folder bug, not a crash. | `raw_dir = Path("../data/raw").resolve()assert raw_dir.exists()` | path setup, future notebooks | pathlib / `.resolve()` question | none |
| L-010 | 2026-08-05 | time series | `Timedelta.total_seconds()` keeps fractional time; `.days` truncates it | “explain `print('Duration (days):', round(...))`” | `(p[ts].max() - p[ts].min())` is a `Timedelta`. `.total_seconds() / 86400` converts it to fractional days (13.94), while the simpler `.days` attribute floors it to 13 and silently drops 22.5 hours. Matters because CGM recordings rarely start/stop at midnight, and the audit needs the real duration, not a rounded-down one. | `delta.total_seconds() / 86400` → `13.9375`, vs `delta.days` → `13` | `01_data_audit.ipynb` timestamp regularity check | duration line walkthrough | none |
| L-011 | 2026-08-05 | data forensics / units | Value *granularity* reveals unit provenance — the spacing between distinct values, not just their magnitude | `[[ backfilled — original question not recorded ]]` | Every CGM reading is an exact multiple of 1.8. Since 1.8 mg/dL = 0.1 mmol/L, the file was recorded in mmol/L to one decimal place and multiplied by 18 before distribution. Magnitude alone (roughly 90–220) only says “mg/dL”; the quantisation says “mg/dL *converted from* mmol/L”, which is a stronger and more defensible claim. | `sorted(df["glucose"].unique())[:5]` → 39.6, 41.4, 43.2, 45.0 — steps of 1.8 | `notebooks/01_data_audit.ipynb`, unit check (D-005) | Phase 0 audit | none |
| L-012 | 2026-08-05 | measurement / statistics | Left censoring — the lowest value repeats exactly because it’s a sensor floor, not physiology | `[[ backfilled ]]` | 39.6 mg/dL (2.2 mmol/L) is the reporting limit of the device. True glucose below that is recorded *at* the limit, not below it. Consequences: TBR is a systematic underestimate, SD and CV are compressed in the hypo range, and a true cohort minimum cannot be computed at all. A spike in the value counts at the minimum is the signature. | `df["glucose"].value_counts().sort_index().head()` — mass piled on the smallest value | Phase 2 TBR / CV definitions | Phase 0 audit | yes — this caveat has to appear in `metric-changelog.md` when TBR and CV are specified |
| L-013 | 2026-08-05 | clinical units | Two unit systems for the same analyte that are *not* a simple scale factor apart | `[[ backfilled ]]` | HbA1c comes in NGSP % (roughly 5–12) and IFCC mmol/mol (roughly 40–90). This dataset is IFCC. The conversion is affine, not multiplicative, so no amount of dividing gets you there. It matters because GMI is *defined* on the NGSP % scale — a Bland–Altman of GMI against a raw mmol/mol column compares two different scales and returns a bias that means nothing. | `hba1c_ngsp = hba1c_ifcc / 10.929 + 2.15` | D-005; Phase 3 Bland–Altman | Phase 0 audit | none |
| L-014 | 2026-08-09 | missingness | `isna()` only sees what pandas *parsed* as null — a sentinel string is ordinary data | `[[ backfilled ]]` | 16 of the 33 clinical summary columns use `/` to mean missing. Those columns are dtype `object`, `isna()` returns 0%, and the missingness table reports a spotless dataset. Sentinel discovery has to run *before* the missingness table: check dtypes, then `value_counts()` on every object column, and look for anything that isn’t a plausible measurement. Then either declare it at read time or replace it. | `df.select_dtypes("object").apply(lambda s: s.value_counts().head(3))pd.read_excel(path, na_values=["/"])` | D-007; missingness table rebuild | Phase 0 audit | none |
| L-015 | 2026-08-09 | pandas / combining tables | `pd.concat` takes the *union* of column names on exact string match — it validates nothing | `[[ backfilled ]]` | The two summary sheets differ by capitalisation, internal spacing, and a trailing space on the eGFR header. `concat` doesn’t raise; it quietly produces a 35-column frame containing near-duplicate pairs, each roughly half null. `repr()` on the headers is what makes invisible whitespace visible. The defence is normalise-then-assert, not eyeballing. | `[repr(c) for c in df.columns]df.columns = df.columns.str.strip().str.lower()assert set(t1.columns) == set(t2.columns)` | D-008; `src/load.py` | Phase 0 audit | none |
| L-016 | 2026-08-10 | time series | Row count × sampling interval is a *prediction*; the timestamps are the *measurement* | `[[ backfilled ]]` | 2029_0: 13 blank rows at 15-minute spacing predicts a 3.50 h gap. The timestamps say 3.533 h. The discrepancy only exists if you measure it. Second finding from the same check — the blank rows carry sub-second drift, which a device ticking on a 15-minute schedule would not produce, so those timestamps were generated to fill the hole rather than recorded by the sensor. Never trust a timestamp column just because it is regular. | `p[ts].diff().dt.total_seconds().div(3600).max()p[ts].dt.second.value_counts()` | 2029_0 device-swap handling; `src/clean.py` gap logic | Phase 0 audit, cross-checked against `note.txt` | none |
| L-017 | 2026-08-11 | schema drift | One file’s columns are not “the schema” — count layouts across all files before writing the loader | `[[ backfilled ]]` | 125 CGM files, 5 distinct column layouts. Two files have the units stripped out of the header text, so any rename dict keyed on the full header string misses them silently. The check is cheap: hash the column tuple per file and count the distinct values. Doing it in the audit is a one-hour job; discovering it mid-loop in Phase 1 is a rewrite. | `Counter(tuple(pd.read_excel(f).columns) for f in cgm_files)` | `01_data_audit.ipynb` §4; `src/load.py` | Phase 0 audit | confirm the 4 non-standard files are named explicitly somewhere in the audit — was this closed? |
| L-018 | 2026-08-11 | dicts / silent failure | A rename dict fails silently — a key that never matches simply does nothing | `[[ backfilled ]]` | Two real bugs in `RENAME_CGM` / `RENAME_SUMMARY`: a `pmol_l` label where the column is `mg_dl`, and a double space inside one T2 duration key. Neither raises. The column keeps its raw name and the failure surfaces hundreds of lines later as a `KeyError` with no connection to the cause. Two assertions catch the whole class: nothing left unmapped, and no two keys colliding on one target. | `assert not set(df.columns) - set(RENAME)assert len(set(RENAME.values())) == len(RENAME)` | `src/load.py::apply_rename` | Phase 1 gate review | confirm both assertions are actually in `apply_rename` |
| L-019 | 2026-08-11 | workflow | `%autoreload 2` — edit `src/` and the notebook picks it up without a kernel restart | `[[ backfilled ]]` | `import` caches a module, so editing `src/load.py` after importing it changes nothing until the module is reloaded. `%autoreload 2` re-imports changed modules before each cell. This is what makes “prototype in the notebook, then move it to `src/`” cheap instead of a restart-and-rerun cycle every time. | `%load_ext autoreload%autoreload 2` | `notebooks/02_cleaning_pipeline.ipynb` | Phase 1 setup | none |
| L-020 | 2026-08-12 | python / docstrings | A docstring only “counts” if it’s the first statement inside the function body, indented under `def` — not floating above it | “what do you want me to do with docstring I don't understand” | A triple-quoted string sitting above `def` at module level doesn’t attach to the function at all — `func.__doc__` comes back empty. Has to be the first line inside the function body, one indent level in. | `def f():` / `    """docstring"""` / `    ...` | `src/metrics.py`, `mean_glucose` and every metric after it | docstring placement fix | none |
| L-021 | 2026-08-12 | pandas / NaN behavior | Every comparison against `NaN` returns `False`, except `!=` which returns `True` — breaks a boolean-mask metric unless gap rows are dropped first | building `tir_glucose`, then again finding `mage_glucose`’s turning-point boundaries | `np.nan >= 70` and `np.nan <= 180` both evaluate to `False`, not `NaN` — a mask built straight from a column with gaps silently counts those gaps as “not in range” instead of excluding them, same effect as imputing zero. `.dropna()` has to happen *before* the mask, not after. Same rule, opposite exception, explained why `diff_sign != diff_sign.shift(-1)` coincidentally flagged the first/last row as turning points (M-006) — `!=` against `NaN` is always `True`. | `(df["cgm_mg_dl"] >= 70) & (df["cgm_mg_dl"] <= 180)` counts `NaN` rows as `False`, not excluded | `tir_glucose`, `qualifying_swings` turning-point detection | TIR gap-handling trap | none |
| L-022 | 2026-08-12 | pathlib / tooling | A relative path like `"../src"` depends on the *current working directory*, not the file’s own location — `__file__` fixes it | “how make from metrics import mean_glucose work in this file” | Works in the notebook by accident (Jupyter’s cwd is `notebooks/`), breaks under pytest (cwd is the repo root). `Path(__file__).resolve().parent.parent / "src"` builds the path relative to the file itself, correct regardless of where the command was run from. | `sys.path.append(str(Path(__file__).resolve().parent.parent / "src"))` | `tests/test_metrics.py` | metrics import fix | none |
| L-023 | 2026-08-12 | pytest / testing | `assert` plus a `test_` name is the entire mechanism — pytest has no special assertion syntax of its own | “teach me what code I can write in test_metrics to check if it return 150” | `assert` is plain Python: `True` does nothing, `False` raises `AssertionError`. pytest just runs every function named `test_...` and reports a fail if an `AssertionError` comes out. Confirmed independently by actually running `pytest tests/test_metrics.py -v` from the terminal, not just trusting that the code looked right. | `assert mean_glucose(df) == 150.0` | `tests/test_metrics.py` | pytest mechanism walkthrough | none |
| L-024 | 2026-08-12 | pandas / groupby | `groupby(...).apply(fn)`, where `fn` returns a `pd.Series`, turns each group into one row and each Series label into a column | building the patient-level metric table | `fn` receives one sub-DataFrame per group — same shape every `metrics.py` function already expects, just called `group` instead of `df`. Returning a labelled `pd.Series` (instead of one scalar) is what produces a multi-column summary table in a single call, instead of one `groupby` per metric. | `df.groupby(["subject","visit"]).apply(lambda g: pd.Series({"mean": mean_glucose(g), ...}))` | `03_cgm_metrics.ipynb`, `summarize()` | groupby/apply toy example, then the real 7 (later 10) functions | none |
| L-025 | 2026-08-12 | pandas / indexing | `.iloc[]` selects by position (first/last row), `.loc[]` by label — the difference matters once `.dropna()` leaves gaps in the index | writing `turning_point.iloc[0]`/`.iloc[-1]` | After `.dropna()` removes gap rows, the surviving index isn’t renumbered (could be `[0,1,3,4,...]`). `.iloc[0]`/`.iloc[-1]` still mean “the actual first/last row” regardless of which label survived; `.loc[0]` would break if row `0` itself had been dropped. | `turning_point.iloc[0] = True` / `turning_point.iloc[-1] = True` | `mage_glucose` explicit turning-point boundaries (M-006) | `.iloc` vs `.loc` explanation | none |
| L-026 | 2026-08-12 | pandas / parquet | `to_parquet(index=False)` on a groupby result drops the MultiIndex *entirely*, not just hides it — `subject`/`visit` were missing from the saved file, not just unlabelled | “check it again I just saved it” | `groupby(["subject","visit"])` puts `subject`/`visit` into the index, not columns. `index=False` means “don’t save the index” — with nothing else holding those identifiers, they’re gone from the file. `.reset_index()` moves them into real columns first. Confirmed by deleting the saved file and regenerating it from a clean run, not just re-reading what was already there. | `summarize_table_a.reset_index().to_parquet(path, index=False)` | `03_cgm_metrics.ipynb`, section 3 save | patient-metrics save bug | none |
| L-027 | 2026-08-12 | code structure | Logic shared by two functions belongs in one helper, not copy-pasted twice — same reuse principle as `cv_glucose` calling `mean_glucose`/`std_glucose`, just applied to a function of its own | refactor before writing `excursion_count` | `mage_glucose` and `excursion_count` both needed the same turning-point-detection + 1×SD filter steps. Pulled into `qualifying_swings(df)` so a future change to that logic can’t update one function and silently miss the other. | `def qualifying_swings(df): ...` — both `mage_glucose` and `excursion_count` call it | `src/metrics.py` | mage/excursion_count refactor | none |
| L-028 | 2026-08-12 | python syntax | The multiplication sign `×` (U+00D7) is not the same character as the asterisk `*` — Python only recognizes the latter as an operator | “check my formula” | Copy-pasting the GMI formula from chat (written with `×` for readability) straight into `gmi_glucose` produced `SyntaxError: invalid character '×'`. Visually near-identical characters can be different code points entirely — the traceback names the exact character. | `gmi = 3.31 + (0.02392 × mean_glucose(df))` → `SyntaxError` | `gmi_glucose` formula | GMI formula typo | none |
| L-029 | 2026-08-12 | statistics / method comparison | Subtraction order for a "diff" column is a convention you fix and state, not something pandas enforces — swapping operand order between two runs silently flips the sign of bias and every downstream stat, no error raised | “because I run this: pair[\"diff\"] = pair[\"hba1c_pct\"] - pair[\"gmi\"]” | Bias printed as -2.31 in one run, +2.31 in the next — same 116 rows, same SD, only the `diff` line changed order. Neither version is mechanically wrong; the missing piece was a fixed, documented convention. Settled on the standard method-comparison convention, test method − reference method (GMI − HbA1c), and added a comment in the cell so the direction can't silently drift on a future re-run. | `pair["diff"] = pair["gmi"] - pair["hba1c_pct"]  # GMI - HbA1c: positive = GMI reads high` | `04_eda_profiles.ipynb` §1, GMI vs. HbA1c agreement | sign-flip debugging, this session | none |
| L-030 | 2026-08-12 | research practice / evidence | A citation isn't verified until it's traced to an actual, readable source — a fabricated paper title and a wrongly-attributed statistic can both sound completely plausible | n/a — surfaced reviewing a pasted interpretation paragraph before it went in the notebook | A drafted paragraph cited "Bridging the Continuous Glucose Monitoring decision gap..." for a 0.5-point HbA1c threshold — that title doesn't exist anywhere searchable. The paper actually named for the NGSP ±0.75% figure (Rohlfing et al. 2014) was real, but didn't contain the 0.5-point claim. A DOI led to the real source instead — Lenters-Westra et al. 2025, free full text on PMC — which does contain both the 0.5% and 0.8% thresholds, word for word. Order of trust: DOI/full-text beats a search-engine summary beats a remembered or pasted claim. | n/a | D-023 interpretation write-up, `04_eda_profiles.ipynb` §1 | citation verification, this session | none |
| L-031 | 2026-08-13 | statistics / method comparison | Bias and limits of agreement (LoA) answer different questions — bias is the average systematic offset across the group, LoA is the spread of *individual* differences. A small bias can still hide an LoA too wide to trust for any one patient. | "explain to me what are those bias and loa mean clinically" | Clinical parallel used: a home BP cuff reading 5 mmHg low on average (bias) is a fixable, predictable offset — but if individual readings range from 10 mmHg low to 3 mmHg high (LoA), the group-average offset doesn't help any one patient's reading. Same logic applied to GMI vs. HbA1c: -2.31 average bias looks almost correctable, but a ~9-point-wide LoA means an individual patient's GMI could still be badly wrong after correcting for the average. | n/a — conceptual, values already in `pair["diff"].mean()`/`.std()` | `04_eda_profiles.ipynb` §1 interpretation paragraph | bias/LoA clinical walkthrough, this session | none |
| L-032 | 2026-08-13 | pandas / boolean masking | Turning a threshold into a patient count has two equivalent routes — filter-then-`len()`, or mask-then-`.sum()`/`.mean()` directly, since `True`/`False` evaluate as `1`/`0` | walking through counting patients above the 0.5% and 0.8% discordance thresholds | `pair[pair["abs_diff"] > 0.5]` filters down to a smaller table; `len()` on that gives the count. `pair["abs_diff"] > 0.5` alone is already a `True`/`False` Series — `.sum()` on it gives the same count with no intermediate table, `.mean()` gives the proportion directly. Both are correct; the mask route skips a step. | `above_0_5 = pair[pair["abs_diff"] > 0.5]; len(above_0_5)` vs. `(pair["abs_diff"] > 0.5).sum()` | `04_eda_profiles.ipynb` §1, discordance counts (92/116, 78/116) | empirical threshold counting, this session | none |
| L-033 | 2026-08-13 | clinical statistics / evidence | A published clinical threshold is built for a specific comparison — using one derived for a *different* comparison can systematically over- or under-count, even when the number itself is correctly sourced | "why 0.8" | Researched two thresholds that sound interchangeable but aren't: 0.5% (Little, Rohlfing, Sacks 2011) is a visit-to-visit clinical-decision-point, reused by convenience in early GMI-discordance papers. 0.8% (Lenters-Westra et al. 2025) was derived specifically for single-lab-HbA1c-vs-single-GMI, deliberately widened past 0.5% to exclude the ~0.8-point analytical noise that exists between two labs even when both pass quality assessment. Using 0.5% here would count some of that ordinary lab noise as if it were real GMI disagreement. Logged as D-024 rather than picked silently. | n/a — literature-derived, not code | D-024; `04_eda_profiles.ipynb` §1 individual-level discordance claim | threshold-matching discussion, this session (Little 2011; Lenters-Westra 2025) | none |
| L-034 | 2026-08-13 | statistics / reliability | "Stable" is a measurement-theory concept — reliability — asking whether the same underlying thing, measured twice, agrees. Test-retest, inter-rater, and split-half are the same question, just splitting the data differently; reliability puts a ceiling on validity, since a wobbly instrument can't correlate strongly with anything, including a real outcome. | "tell me what does stability in my dataset mean and what's that mean in general, how can I apply it on other project" | Clinical parallel: an assay's reproducibility (run the same sample twice, expect close-to-identical results) is the same idea. Applied to CGM: does a metric computed on the first half of a recording agree with the same metric on the second half — a real patient characteristic, or an accident of which portion got captured. Transferable rule for future projects: before trusting any aggregate over a noisy/variable window (rolling average, rate, count), check whether it holds up when computed from a different overlapping slice of the same data. | n/a — conceptual grounding before D-025/D-026 | D-025, D-026; `04_eda_profiles.ipynb` §2 | stability concept walkthrough, this session | none |
| L-035 | 2026-08-13 | statistics / ICC | Intraclass Correlation Coefficient (ICC) is a ratio of between-subject variance (BMS) to within-subject variance (WMS) — worked out by hand on a toy example before any code, since it's real new machinery, not a small extension of mean/std | "second toy example" (walked two toy examples: one moderate ~0.46, one near-perfect ~0.99) | BMS: each patient's own mean vs. the grand mean, squared, summed, ×k, ÷df. WMS: each patient's own values vs. their own mean, squared, summed, ÷df. `ICC = (BMS − WMS) / (BMS + WMS)`. On real data already in wide format (one row per patient, `h1`/`h2` columns), this simplifies: `bms = k * own_mean.var()`, `wms = 0.5 * ((h1 - h2) ** 2).mean()` — no groupby needed, since the "grouping" is already the row. | `own_mean = (h1+h2)/2; bms = 2*own_mean.var(); wms = 0.5*((h1-h2)**2).mean(); icc = (bms-wms)/(bms+wms)` | `04_eda_profiles.ipynb` §2, `icc_table` | ICC toy-example walkthrough, this session | none |
| L-036 | 2026-08-13 | statistics / reliability | Which ICC "form" to use is itself a decision, not a default — a two-way "consistency" form factors out any systematic offset between the two things compared before scoring reliability; a one-way form folds a systematic offset into the penalty instead | closing the open question left in D-026 | Chose the one-way (punishing) form specifically because `mean_glucose` had already shown a real, systematic +10.7 bias between first-half and second-half (D-024's own split-half check) — a metric that predictably drifts within one recording still isn't a stable endpoint, so the form that excuses "consistently wrong in the same direction" was the wrong choice here. | n/a — reasoning applied to the formula in L-035 | D-026 | ICC form decision, this session | none |
| L-037 | 2026-08-13 | statistics / measurement theory | A fixed linear (affine) transformation of a variable is mathematically guaranteed to share its correlation-based statistics with the original — ICC, like Pearson r, is a ratio of variances, and a fixed scale/shift cancels out of a ratio identically | surfaced reviewing the full `icc_table` output, not asked directly | `mean` and `gmi` landed on the exact same ICC (0.703155) across all 125 subject-visits. Not a coincidence and not a bug (unlike L-039's self-subtraction bugs, which also produced suspiciously clean numbers) — GMI is defined as `3.31 + 0.02392 × mean_glucose` (M-004), a straight linear transform, so its reliability score is forced to equal mean glucose's for any dataset. | n/a — conceptual, confirmed against the formula, not a new code pattern | D-026 | mean/gmi ICC-identity finding, this session | none |
| L-038 | 2026-08-13 | pandas / indexers | `.loc` is technically callable with `()` (a legacy axis-binding feature), so calling it with parentheses instead of indexing it with square brackets doesn't raise an error — it silently returns the wrong object type entirely, worse than a crash | debugging `one_sub = merged.loc(["subject"]==2035)` | Tested directly: `df.loc(anything)` returns a `_LocIndexer` object, not a filtered DataFrame or a `TypeError` — same "pandas quietly does *something* instead of raising" family as L-015 (`concat`) and L-018 (rename dict), just on an indexer. Correct form uses square brackets with a real boolean mask built off a column: `df[df["subject"] == 2035]`, not `df.loc(["subject"]==2035)` (a list compared to a number, always `False`, never even reaches the data). | `merged.loc(["subject"]==2035)` → wrong type, no error, vs `merged[merged["subject"]==2035]` → correct | `04_eda_profiles.ipynb` §2, one-subject filtering | `.loc()` vs `.loc[]` debugging, this session | none |
| L-039 | 2026-08-13 | debugging / pandas | A result landing on a suspiciously "clean" exact number (0.0, or two values matching to the last decimal) is itself a diagnostic clue — real measured data essentially never produces an exact round number by coincidence, so treat it as a bug signal before reporting it as a finding | "bias 0.0 is that right?" | Same root mistake happened twice: `mean_diff = mean_h1 - mean_h1` (should've been `mean_h2`) — any column minus itself is exactly 0 regardless of the data, which is what gave it away. The identical typo recurred in the ICC cell (`own_mean`/`wms` built from `mean_h1` twice), which would have forced ICC to exactly 1.0 had a stale-output check not caught it first. Suspicious-exactness became a standing check before trusting any number this session, alongside "does the traceback make sense." | `mean_h1 - mean_h1` (always 0, any data) vs `mean_h1 - mean_h2` (real difference) | `04_eda_profiles.ipynb` §2, bias/spread and ICC cells | repeated self-subtraction bug, this session | none |
| L-040 | 2026-08-13 | tooling / package management | A package's install *metadata* can say it's installed even when the actual files are gone — `ensurepip`/`pip` check the record, not the files | "still show not module named pip" | `.venv-air`'s `pip-26.2.1.dist-info` folder (the install record) existed with no actual `pip/` package folder behind it — `ensurepip --upgrade` saw the matching version in the record and skipped reinstalling, doing nothing. Deleting the stale dist-info folder first forced a real reinstall. Same root lesson as L-015 (don't trust what a tool *says* is there without checking) applied to environment tooling instead of data. | `rm -rf .venv-air/lib/.../pip-*.dist-info` / `python3 -m ensurepip --upgrade` | `.venv-air` repair, this session | matplotlib/pytest install debugging | none |
| L-041 | 2026-08-13 | statistics / method comparison | Absolute variability metrics (SD, MAGE) partly reflect *where* a value sits, not just how much it swings — %CV divides the mean back out and isolates the "pure" swing | "why does std separate T1DM/T2DM more cleanly than cv" | T1DM's mean glucose (163.6 mg/dL) runs higher than T2DM's (141.6) in this cohort. SD/MAGE, being absolute mg/dL, inherit some of that baseline-level gap alongside the real variability gap; %CV (SD/mean) divides the mean back out, so its between-group gap (36% relative) is smaller and closer to a "pure" variability comparison than SD's (54%). Same clinical logic as comparing BP variability in mmHg between a patient averaging 110 and one averaging 180. | `cv_glucose = std_glucose / mean_glucose` | `04_eda_profiles.ipynb` §3 interpretation | SD-vs-CV discussion, this session | none |
| L-042 | 2026-08-13 | machine learning / preprocessing | Distance-based algorithms (k-means, k-NN, PCA) sum squared differences across *all* features at once — a feature with bigger raw numbers dominates the distance regardless of whether it's actually more important | "walk me through I don't know anything about k means" | Toy example: patient X vs Y differ by 5 on both cv and mage; X vs Z differ by 0 on cv, 60 on mage. Raw Euclidean distance X-Z (60) swamps X-Y (7.1) almost entirely from mage, even though a 5-point cv gap is proportionally larger given cv's real range. Scaling (subtract mean, divide by SD) puts every feature in comparable units before distance is computed. Same logic as why comparing WBC count and body temperature directly (no scaling) would let WBC dominate purely from bigger raw numbers. | `sqrt((cv_diff)**2 + (mage_diff)**2)` — unscaled, mage's bigger range dominates | `04_eda_profiles.ipynb` §4, D-031 | k-means scaling walkthrough, this session | none |
| L-043 | 2026-08-13 | machine learning / preprocessing | Min-max scaling anchors its 0-1 range to a single min/max value — one real outlier compresses every "normal" value into a tiny sliver; z-score is nudged by outliers too but doesn't hard-anchor to one point | "is z score the most suitable here" | Checked before deciding rather than assuming: `tbr` is heavily skewed in this cohort (median 0.49, tail to 67.25, 17/125 flagged outliers by IQR rule). Min-max would turn that one 67.25 value into "1.0" and squeeze the 100+ typical patients (clustered 0-2) into a near-invisible sliver. Z-score doesn't have that specific failure mode, though it doesn't fully fix the underlying skew either — logged as a disclosed limitation (D-031), not solved. | n/a — decision, not code yet | D-031 | scaling method decision, this session | none |
| L-044 | 2026-08-13 | scikit-learn / API pattern | Every sklearn tool follows fit → transform (or fit_predict) instead of pandas' "just compute and return" style — `fit` learns something from data, `transform`/`predict` applies it | "walk me through" (sklearn calls) | `StandardScaler().fit_transform(X)` learns each column's mean/SD from X, then applies the subtract-divide. `KMeans(...).fit_predict(X)` finds cluster centers, then assigns every row to its nearest one, both in one call. `silhouette_score(X, labels)` takes the scaled data *and* the labels together, returns one float. Same two-step logic as establishing a lab reference range from a population, then scoring one patient against it. | `scaler.fit_transform(X)` / `km.fit_predict(X_scaled)` / `silhouette_score(X_scaled, labels)` | `04_eda_profiles.ipynb` §4 | sklearn walkthrough, this session | none |
| L-045 | 2026-08-13 | pandas / method chaining | `.dropna()` (and most pandas methods) return a *new* object — calling it on a line by itself with no assignment changes nothing, same trap as L-004 in a new API | reviewing own code: `X.dropna()` with no assignment | Wrote `X.dropna()` expecting it to clean `X` in place; it computed a cleaned copy and discarded it. Didn't crash because these 4 columns happen to have zero NaNs in this cohort — silently wrong, not loudly wrong. Fix: `X = X[[...]].dropna()`. | `X.dropna()` (does nothing) vs `X = X.dropna()` (correct) | `04_eda_profiles.ipynb` §4 k-means cell | own-code review, this session | none |
| L-046 | 2026-08-13 | pandas / groupby | `.groupby(...)` alone returns a `DataFrameGroupBy` object, not a DataFrame — it needs an aggregation (`.mean()`) or `.apply()` before it's usable as a table | debugging `ValueError: setting an array element with a sequence... inhomogeneous shape` | Wrote `X = merged.groupby("diabetes_type")[[...]]` meaning to just select 4 columns, leftover from an earlier edit. Passed to `StandardScaler`, which tried to convert it to an array — iterating a groupby object gives `(group_name, group_dataframe)` pairs, not rows, which is where the "inhomogeneous shape (2, 2)" error came from (2 diabetes types, each pair looking like 2 mismatched things). Fix: drop `.groupby(...)`, just `merged[[...]]`. | `merged.groupby("diabetes_type")[[...]]` (GroupBy object) vs `merged[[...]]` (DataFrame) | `04_eda_profiles.ipynb` §4 k-means cell | k-means crash debugging, this session | none |
| L-047 | 2026-08-13 | python / built-ins | `max(iterable, key=...)` lets you pick the *largest-by-some-other-value* item while still getting back the original item, not the value you judged it by | "actual syntax" (picking best k from a scores dict) | `max(scores)` returns the biggest dict *key*; `max(scores.values())` returns the biggest *value* but loses which key it came from. `max(scores, key=scores.get)` judges each key by its looked-up value but returns the key itself — same `key=` idea as `sorted(..., key=...)`. | `best_k = max(scores, key=scores.get)` | `04_eda_profiles.ipynb` §4, k-selection | silhouette k-selection, this session | none |
| L-048 | 2026-08-13 | pandas / deduplication | `drop_duplicates(subset=..., keep="first")` keeps only the first row per group of duplicate values on the named column(s) — "first" means first in current row order, so sort first if order matters | building the repeat-visit robustness check | Needed one row per subject (first visit only) to test whether k-means' cluster structure was an artifact of 10 subjects contributing 2-3 correlated visits each. `sort_values(["subject","visit"])` before `drop_duplicates(subset="subject", keep="first")` guarantees "first" really means earliest visit, not just whatever order the DataFrame happened to be in. | `merged.sort_values(["subject","visit"]).drop_duplicates(subset="subject", keep="first")` | `04_eda_profiles.ipynb` §4, D-032 addendum robustness check | robustness-check build, this session | none |
| L-049 | 2026-08-13 | pandas / SettingWithCopyWarning | Assigning a new column onto a DataFrame that's a *slice* of another (e.g. straight out of `drop_duplicates()` or boolean filtering) can trigger `SettingWithCopyWarning` — `.copy()` makes it an independent DataFrame first | building the robustness check cell | `reduced = merged.sort_values(...).drop_duplicates(...)` is a slice of `merged`; later doing `reduced["cluster"] = labels` on it is ambiguous to pandas (is this meant to modify `merged` too?). `.copy()` right after the slice removes the ambiguity. | `reduced = merged.sort_values([...]).drop_duplicates(...).copy()` | `04_eda_profiles.ipynb` §4 robustness check | flagged pre-emptively, not yet hit live | confirm this actually fires when `reduced` is used without `.copy()` — not yet directly observed, only anticipated |
| L-050 | 2026-08-13 | machine learning / clustering pitfalls | Silhouette score can be *maximized* by a degenerate split — an isolated single-point "cluster" has near-zero within-cluster distance and huge between-cluster distance, so silhouette rewards outlier-isolation even when it means nothing clinically | "best_k = 2" result review | k=2 scored highest (0.744) but turned out to be 124 patients vs. 1 (subject 2077, whose tbr=67.25 is the cohort's most extreme value). k=3 scored lower (0.425) but gave three real, interpretable groups. Confirmed the pattern wasn't a fluke: k=8 also produced a singleton cluster. This is why D-032 built a physiological check *on top of* the raw statistic rather than trusting argmax(silhouette) blindly — same discipline as D-019 not trusting a formula's eGFR output without checking physiology first. | n/a — investigative, not a single line of code | D-034 | outlier-cluster investigation, this session | none |
| L-051 | 2026-08-13 | statistics / feature selection | Features can be *exactly* collinear without looking like it at a glance — check correlations (and known constraints, like percentages summing to 100) before feeding a feature set into a distance-based method | reviewing D-030's original 6-feature set | `tir + tar + tbr = 100.0` exactly, every row — confirmed by direct computation, not assumed from knowing what the metrics mean. That's 2 degrees of freedom presented as 3 columns, silently overweighting that axis in Euclidean distance. Separately, `std` and `mage` correlate at 0.92 — different algorithms, same underlying signal. Neither was obvious without actually running `.corr()` on the candidate features first. | `merged[cols].corr()` — found tir/tar/tbr and std/mage redundancy | D-033 (superseded into D-030) | correlation check before k-means, this session | none |
| L-052 | 2026-08-17 | statistics / method comparison | Bland-Altman's flat bias/LoA is a cohort average — the disagreement itself can depend systematically on the measured value ("proportional bias"), which a single mean/SD can't detect | n/a — surfaced from build_state.md's open item on the Bland-Altman plot's visual pattern | Regressing the GMI-HbA1c diff against HbA1c gave slope -0.87, r=-0.96, p≈1.6×10⁻⁶³ — for every 1-point rise in true HbA1c, the disagreement widened by ~0.87 points. A flat bias number (like the -2.31% already reported) is only representative near the middle of the range, not at the extremes. | `linregress(pair["hba1c_pct"], pair["diff"])` → `.slope`/`.pvalue`/`.rvalue` | D-037; `04_eda_profiles.ipynb` §1 | proportional bias walkthrough, this session | none |
| L-053 | 2026-08-17 | statistics / method comparison | Bland-Altman's standard x-axis (mean of the two methods) can create a spurious diff-vs-mean correlation on its own, since both quantities are built from the same two input numbers — this "mathematical coupling" has to be ruled out before trusting a proportional-bias finding | "checked against a mathematical-coupling artifact" — verifying the finding wasn't a plotting artifact before trusting it | Reran the same regression using the reference method alone (HbA1c) instead of mean-of-pair as the x-axis. The pattern held and got stronger (r went from -0.87 to -0.96) — a coupling artifact would have weakened or disappeared under this check, not strengthened, which is what rules it out. | `linregress(pair["hba1c_pct"], pair["diff"])` vs `linregress(mean_of_pair, pair["diff"])` | D-037 | coupling-artifact check, this session | none |
| L-054 | 2026-08-17 | statistics / measurement theory | A fixed linear transform of a variable can't spread out more than the original does — comparing standard deviations between two methods reveals whether one is structurally compressed relative to the other, which can produce exactly the kind of "widening disagreement" pattern seen as proportional bias | n/a — dug into why the proportional-bias slope was so strong before writing it up | GMI's std (0.73) is roughly a third of lab HbA1c's std (2.51) in this cohort. Since GMI = 3.31 + 0.02392×mean_glucose is a fixed linear formula, it inherits only as much spread as mean CGM glucose has — it structurally can't represent the full range of glycemic control lab HbA1c can, especially at the severe end. | `pair["gmi"].std()`, `pair["hba1c_pct"].std()` | D-037 | variance-compression explanation, this session | none |
| L-055 | 2026-08-17 | statistics / evidence practice | A pattern needs to be checked as "real across the whole range" vs. "a few outliers pulling an average" before it's trusted — binning into quantiles, and re-checking after truncating the extreme values, are two independent ways to test this | "dig further" | Checked two ways: quintile-binned mean diff moved smoothly from -0.31 to -6.03 points with no single bin driving it, and restricting to HbA1c ≤ 8% alone (cutting out the most severe patients) still showed a significant, though weaker, slope (-0.35, p=0.0019). Together these ruled out "a handful of very sick patients wrecking the average" as the explanation. | `pd.qcut(pair["hba1c_pct"], 5)`; `linregress` rerun on `pair[pair["hba1c_pct"] <= 8]` | D-037 | outlier-vs-cohort-wide check, this session | none |
| L-056 | 2026-08-17 | study design / measurement | "Predicts" and "is associated with" split on one thing — whether the predictor genuinely came *before* the outcome in time, or was just sitting in the same record at the same time | "explain the framing part clearer... I want a full grasp on what exactly this model is for" | Clinical parallel: a prospective cohort measures baseline, follows forward, then checks who develops the outcome — that ordering is what earns the word "predicts." A case-control/cross-sectional design compares people who already have the outcome to people who don't, using data from the same snapshot — real association, no earned "predicts" claim, since nothing establishes which came first. `has_hypoglycemia` is a chart/questionnaire flag with no recorded timing relative to the CGM window, so this dataset is structurally the second kind — which is exactly what D-005 already committed to, just not previously understood this concretely. | n/a — conceptual | Phase 4 model framing; `findings.md` | framing walkthrough, this session | none |
| L-057 | 2026-08-17 | machine learning / classification | A decision tree is an automatically-built version of a clinical decision algorithm — a chain of feature-threshold splits ending in a leaf that reports "what fraction of training examples like this were positive" | "teach me what is a tree model" | Clinical parallel: a triage flowchart (febrile? → yes → heart rate >120? → escalate). Toy example built on real columns: split on `tbr > 5%`, then `mean_glucose < 150`, each leaf reporting a training-data hypo-positive rate. Difference from a real protocol: the algorithm searches every feature/cutoff to find the split that best separates yes/no, nobody designs the branches by hand. A single tree overfits (can carve a rule that fits one training patient exactly); ensembles (random forest, gradient boosting/XGBoost) fix this by combining many trees — why the plan says "tree ensembles," not "a tree." | n/a — conceptual, precedes any code | `05_modeling.ipynb` planning | tree model walkthrough, this session | none |
| L-058 | 2026-08-17 | machine learning / classification | A classifier's 0-1 output is a resemblance/confidence score, not a measured value — same shape as a clinical risk score (PSA, Framingham), where the number itself isn't observed, a formula produces it | "what does one number between 0 and 1 mean" | Toy single-feature example: low TBR → output near 0.05, TBR ~7% → ~0.65, TBR 20%+ → ~0.95. 0 = confident no, 1 = confident yes, 0.5 = genuine coin-flip. With many real inputs, the model combines all of them into one weighted score, squeezed into the 0-1 range — the weighting is exactly what training learns, and exactly what SHAP unpacks per-patient afterward. Confirmed her own restated definition afterward ("how closely this visit's profile resembles the profile of visits already flagged yes") was correct, with one word-choice fix: "possibility" implies forecasting, which D-005's framing (L-056) rules out — "resemblance" is the accurate word. | n/a — conceptual | `05_modeling.ipynb` planning | probability-output walkthrough, this session | none |
| L-059 | 2026-08-17 | clinical pharmacology / feature engineering | Summing a dose across a whole visit confounds "how much medication" with "how long the visit happened to run" — dividing by visit duration removes that confound and matches a real clinical unit (Total Daily Dose) | "think as a physician, which would he suggest" | TDD (total insulin units/24h, all types combined) is the standard clinical framing for insulin regimen intensity and hypoglycemia risk. A 5-day recording accumulates more total IU than a 1-day one on an identical regimen — a visit-length artifact, not a real risk difference — so raw total dose was rejected as the primary feature in favor of dose/day. Injection count kept as a second feature (regimen complexity/dosing-error risk) since it isn't redundant with dose size — 3×2 IU and 3×20 IU are different risk profiles a count alone can't distinguish. | n/a — decision, not code yet | D-038; `src/features.py` | physician-reasoning walkthrough, this session | none |
| L-060 | 2026-08-17 | data quality / missingness | A blank cell isn't automatically "missing" — check whether the row has a complete record elsewhere that explains the blank before defaulting to NaN | "help me choose between log is as 0 or NaN" | Grouped `table_a_clean` by subject-visit and checked, for the 58 visits with no `insulin_dose_sc` entry, whether the CSII or non-insulin-agent columns were populated instead. 53 of 58 had a clear alternative explanation (13 on a pump, 49 on oral agents only) — genuinely 0 SC insulin, not unknown. Only 5/125 (4%) had nothing in any medication column, the real ambiguous case — too small a slice to make the whole column default to NaN over. Same "check the data before deciding" discipline as L-033/L-043, applied to a missingness call instead of a threshold or scaling choice. | `ta.groupby(["subject","visit"])[col].apply(lambda s: s.notna().any())` per medication column, then compared | D-039; `src/features.py` | 0-vs-NaN decision check, this session | none |

---