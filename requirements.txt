pandas>=3.0
numpy>=2.5
openpyxl>=3.1
xlrd>=2.0
pyarrow>=25.0
pytest>=9.1.1
matplotlib>=3.11
scikit-learn>=1.9
scipy>=1.18

# Phase 5 — installed and confirmed working (dashboard smoke-tested 2026-09-13)
plotly>=7.0
streamlit>=1.63

# Phase 4 — not yet installed/tested locally, so unpinned for now
jupyter
xgboost
shap
