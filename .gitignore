# Python
__pycache__/
*.pyc
.pytest_cache/

# Environments — rebuild with `pip install -r requirements.txt`, never stored
.venv/
.venv-air/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# Editor / tool-local config
.claude/settings.local.json

# Personal scratch work -- not part of the repo's real deliverables
scratch/

# Working docs -- for this session, not for a repo reviewer
CLAUDE.md
MetaFlex_Build_Plan_v2.pdf
MetaFlex_Skills_and_Deliverables_v2.pdf

# Data — raw is never committed (repo rule). interim/processed are
# regenerated by the pipeline from raw + src/, so they aren't committed either.
data/raw/
data/interim/
data/processed/
